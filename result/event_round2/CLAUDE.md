# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

此為 Spring Modulith 實作參考專案，包含完整的 Spring Boot 4.0.4 + Spring Modulith 2.0.5 模組化單體架構實作。


## Development Guidelines

### Rules

1. **Don't analyze `docs/` directory** unless explicitly requested by the user
2. **For Java library versions**, always check `central.sonatype.com` with `sort=v+desc` parameter, not `search.maven.org`
3. **For Spring Modulith design**, refer to `docs/references/spring-modulith-reference.md` and inform the user
